fn printf(msg: string, ...) -> int;

fn encode(x: int, y: int) -> long {
    return (x as long << 32L) | y as long
}

fn main() {
    let numbers: long = encode(60, 9)
    let resultado: long = (numbers & 0xFFFFFFFF) + ((numbers >> 32L) & 0xFFFFFFFF)
    printf("resultado: %lld\n", resultado)
}
