import { printf } from std.libc.io

fn main() {
    const size: i32 = 5
    const arr: char*[size] = ["blopa", "orn", "fernando", "olc", "brasil"]
    let i: int;
    while i < size printf("%s\n", arr[i++])
}
